<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_otp_card_default"
    android:padding="@dimen/space_md"
    android:layout_marginBottom="@dimen/space_sm"
    android:clickable="true"
    android:focusable="true">

    <!-- Header: app tag + member tag -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="@dimen/space_xs">

        <TextView
            android:id="@+id/appTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/muted"
            android:fontFamily="sans-serif-medium"
            android:textAllCaps="true"
            android:letterSpacing="0.05" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/memberTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/muted"
            android:fontFamily="sans-serif-medium"
            android:textAllCaps="true"
            android:letterSpacing="0.05" />
    </LinearLayout>

    <!-- OTP code -->
    <TextView
        android:id="@+id/otpCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_otp_code"
        android:textColor="@color/ink"
        android:fontFamily="monospace"
        android:letterSpacing="0.1"
        android:layout_marginBottom="@dimen/space_xxs" />

    <!-- Message body -->
    <TextView
        android:id="@+id/msgBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_small"
        android:textColor="@color/body"
        android:lineSpacingMultiplier="1.4"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginBottom="@dimen/space_sm" />

    <!-- Footer: from + time -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="@dimen/space_sm">

        <TextView
            android:id="@+id/fromText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/border_strong"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/timeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/border_strong" />
    </LinearLayout>

    <!-- TTL progress bar -->
    <ProgressBar
        android:id="@+id/ttlBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/ttl_bar_height"
        android:progressDrawable="@drawable/bg_ttl_progress"
        android:max="1000" />
</LinearLayout>
